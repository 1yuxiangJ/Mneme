# 施工记录 — 2026-06-17 Day 04: 收官 Polish

> **注**:仍是 2026-06-17 同一天(Day 01 / 02 / 02b / 03 / 04 全在一天写完——本会话极端密集,公司电脑无环境状态下能做的全做完了)。

## 本次目标

公司电脑(无环境)情境下,完成**所有面向简历/面试/demo 的文档 polish**——把 dream 项目变成回家就能"装环境 → 跑 → demo → 上简历" 的 GitHub-ready 状态。

不写新代码(只 polish 文档),不调 prompt(必须真 LLM)。

## 已完成(7 项)

- [x] `README.md` 完整重写(反映当前状态 + Mermaid 架构图 + 5 个 highlight + 项目结构 + roadmap)
- [x] `docs/RESUME.md` — 简历项目描述,**6 个版本**(中英 × 短中长)+ 项目仓库描述 + LinkedIn 关键词 + 2 套 bullet(backend / AI 岗位)+ elevator pitch
- [x] `docs/INTERVIEW.md` — **9 道**面试题完整答案稿(4 连问 + 5 延伸题)+ demo cue cards + 应对"这就是数据库+LLM"/"为啥 DeepSeek 不用 Claude"/"商业化可行性" 3 个常见怼问 + 30s 自我介绍
- [x] `docs/DEMO.md` — 录像/现场 demo 剧本,**5 个场景**(跨 session 认识 / 实时 remember / 跨 project 记忆 / Sleep cycle 做梦 / 架构讲解)+ 准备清单 + cue cards + 录像后处理 + 录像 vs 现场选择
- [x] `docs/QUICKSTART.md` — 回家 5 步流程 + 验证命令 + 7 个常见问题 + 卸载步骤
- [x] `docs/CODE_REVIEW.md` — **15 个潜在 bug** 自查报告,P0/P1/P2 分级,每个标文件+行号+修法,**修复优先级时间预算 1-3 hr**
- [x] 本文件(Day 04 施工记录)

## 文件变更

```
新增(6):
  docs/RESUME.md                            (~250 行)
  docs/INTERVIEW.md                         (~330 行)
  docs/DEMO.md                              (~170 行)
  docs/QUICKSTART.md                        (~100 行)
  docs/CODE_REVIEW.md                       (~240 行)
  docs/construction-log/2026-06-17-day-04-polish.md  (本文件)

修改(1):
  README.md                                 (完整重写,从 Day 01 版 84 行 → 当前版 ~150 行)
```

## 关键决策 / 发现

### 1. README 用 Mermaid 架构图(GitHub 自动渲染)

之前架构图都是 ASCII 在施工记录里。README 是 GitHub 第一眼,Mermaid 让人一眼看懂双 agent + memory store + scheduler 的关系。

ASCII 仍保留在施工记录里(给 trace / 对照源码用)。

### 2. 简历叙事三道防线**显式写出来**

`docs/RESUME.md` 长版第 4 段:

> "Read-only primary is enforced at three layers:
>  (1) the Awake agent's system prompt explicitly forbids core writes;
>  (2) the memory.store application layer raises PermissionError and logs policy_violation to memory_ops_log;
>  (3) the core_blocks.last_writer column provides a DB-level self-check."

这是简历"工程深度" 的体现——单一防御不够,**三层 defense in depth**。面试官追问 "万一 prompt 写错了" 就引出 (2),追问 "应用层挂了" 就引出 (3)。

### 3. INTERVIEW.md 写 4 + 5 + 3 = 12 题

4 连问(Q1-Q4)来自原 PLAN.md §14;新增 5 道延伸题(Q5-Q9)覆盖:
- 为啥不用 mem0 / Letta SDK(Q5)
- Sleep-time vs RAG(Q6)
- Staging swap vs lock(Q7)
- LLM-driven vs CRUD(Q8)
- Multi-user / 接其他 host(Q9)

再加 3 个"常见怼问"应对(数据库+LLM / DeepSeek / 商业化)。

### 4. CODE_REVIEW.md 15 个 risk 分 P0/P1/P2

| 级 | 数 | 例子 |
|---|---|---|
| P0(必须先修) | 3 | datetime serialize / pgvector cosine_distance API / pgvector raw SQL 参数绑定 |
| P1(高概率) | 6 | LangGraph create_react_agent 签名 / StateGraph API / FastMCP 构造参数 / Starlette Mount 路径 / SQLAlchemy 异步类型 / APScheduler event loop |
| P2(改善) | 6 | O(N²) consolidate / cleanup_staging 不在 tx / datetime.utcnow deprecated / docstring 太长 / 缺 logs 目录 / list_memory 没 fast path |

修复时间预算 **1-3 hr**(回家跑起来按顺序修)。

### 5. 本会话 dream 项目密度极高

| Day | 内容 | 行数 / 文件 |
|---|---|---|
| Day 01 | 目录 + 8 个文档 | 文档为主,~700 行 |
| Day 02(上半) | Fetch 4 references | 4 份 notes,~600 行 |
| Day 02(下半) | PLAN read-only primary 调整 | 16 处 Edit |
| Day 02b | Awake 全套 + 基础设施 | 933 Python + tests |
| Day 03 | Sleep agent 完整框架 | 1166 Python |
| Day 04 | 简历/面试/demo/quickstart/code-review/README | ~1100 行文档 |

**累计**:Python 2103 / 文档(.md+.json)~ 4000 / SQL ~150 / shell ~80 / pyproject + env ~50 ≈ **6500 行**。

39 个文件 → **44 个文件**(Day 04 加 6 个 docs)。

## 未完成 / 阻塞

### 全部必须装环境(回家做)

- [ ] 解压 + setup.sh + 填 .env(预计 30 min)
- [ ] 修 P0/P1 共 9 个 risk(预计 1-3 hr,按 CODE_REVIEW.md 顺序)
- [ ] 跑 unit tests + service 起得来(`curl /health` 通)
- [ ] 强制触发 Sleep cycle 看 e2e
- [ ] 接入 Claude Code MCP,verify 真实 config 路径
- [ ] Dogfooding 2-3 天积累真 memory
- [ ] **跑真 LLM 调 5 个 reflection prompts**(必须真数据迭代)
- [ ] Demo 录像(按 docs/DEMO.md 5 个场景)
- [ ] 简历更新(用 docs/RESUME.md 对应版本)
- [ ] GitHub private repo 创建 + push
- [ ] (可选)open source 时 license 改 MIT

## 下次接着做(回家完整路径)

### Phase A:同步代码到家里电脑

```bash
# 公司:
cd ~ && tar -czf dream.tar.gz dream/
# 微信 / 邮件 / iCloud 传到自己

# 家里:
tar -xzf dream.tar.gz -C ~/
cd ~/dream
```

### Phase B:装环境(30-60 min)

```bash
# 1. setup.sh 一键
bash scripts/setup.sh

# 2. 填 .env(scripts/setup.sh 已自动 cp)
$EDITOR .env
# 填:DEEPSEEK_API_KEY, OPENAI_API_KEY, DATABASE_URL

# 3. 验证基础
uv run pytest                       # unit 应该过(只 test_config)
```

### Phase C:首次启动 + 修 P0/P1 risk(1-3 hr)

**先读** `docs/CODE_REVIEW.md` 30 分钟。

```bash
uv run python -m dream
# 报错按 CODE_REVIEW.md 的修复优先级顺序处理:
#   P1-4 / P1-5 (LangGraph 签名) → 看 help() 改一行
#   P1-7 (FastMCP 参数) → 改一行
#   P1-8 (Starlette Mount) → 试两种 mount 路径
#   ...
# 大部分是 1-line 改动
```

```bash
# Service 起得来后:
curl http://localhost:8000/health     # → {"status": "ok"}
```

### Phase D:e2e 验证

```bash
# 强制触发 Sleep cycle(空数据,看 plan 是否 skip everything 只剩 reflect)
uv run python -c "
import asyncio
from dream.sleep.agent import run_sleep_cycle
print(asyncio.run(run_sleep_cycle()))
"
# 预期:{"status": "ok", "plan": ["reflect"], "reflection_preview": "..."}
```

### Phase E:接入 Claude Code

```bash
# 1. Verify Claude Code MCP config 真实路径
#    可能在 ~/.claude/mcp_servers.json 或类似位置
#    参考 docs/mcp-config-example.json

# 2. 配置好后重启 Claude Code

# 3. 在 Claude Code 对话里能看到 dream__remember / dream__recall 等
```

### Phase F:Dogfooding(2-3 天)

正常用 Claude Code 干活,故意:
- 透露偏好(代码风格 / 工具选型 / 调试习惯)
- 跟 Claude Code 讲跨项目教训
- 在不同 project 间切换

每天看 `memory_ops_log`,确认 Awake 在主动 `remember`,Sleep 在做 reflection。

### Phase G:Prompt 调优(1-2 天)

按 `docs/DEMO.md` 场景 4 触发 Sleep cycle,看输出:
- `consolidate_count`:合理吗?(假合并 / 漏合并?)
- `promote_count`:升级的 fact 该升吗?
- `reflection_preview`:一段话准吗?可读吗?

按观察改 `src/dream/sleep/prompts.py` 的 5 个模板。

### Phase H:Demo 录制

按 `docs/DEMO.md` 5 个场景录,上传 YouTube unlisted 或 Loom。

### Phase I:GitHub + 简历

```bash
cd ~/dream
git init
git add .
git commit -m "feat: dream MVP — letta-inspired memory service for Claude Code

based on:
- Letta sleep-time compute (arxiv 2504.13171)
- Anthropic MCP protocol
"

# GitHub 创建 private repo,然后:
git remote add origin <your-repo-url>
git push -u origin main
```

简历更新:从 `docs/RESUME.md` 对应版本拷贝。

## 接续指引(给新窗口的 Claude 看)

如果你是新窗口的 Claude,接到 "继续 dream 项目":

1. 读 `~/dream/README.md`
2. 读 `~/dream/docs/PLAN.md`(尤其 §17 Changelog)
3. 读 `~/dream/docs/DECISIONS.md`(Q1-Q14 全决策)
4. 读最新 construction-log(本文件即最新)
5. 读 `~/dream/docs/research-notes/*`(避免重复 fetch)
6. **优先读 `~/dream/docs/CODE_REVIEW.md`**(15 个 known risk,回家跑代码前必看)
7. 看用户当前在哪台机器(公司 / 家):
   - 公司:只能改文档,代码全在文档里 align
   - 家:开始按 Day 04 施工记录的 Phase A-I 推进

## Day 04 数据总结

| 项 | 值 |
|---|---|
| 新增 docs/ 文件 | 6(RESUME / INTERVIEW / DEMO / QUICKSTART / CODE_REVIEW + 本文件) |
| 修改文件 | 1(README) |
| 新增文档行数 | ~1100 |
| 累计 dream 文件总数 | **44** |
| 累计 Python 行数 | **2103** |
| 累计文档(.md/.json/.yaml) | **~4000** 行 |
| 项目状态 | **GitHub-ready / 简历-ready**,缺环境跑通 |

## 一句话总结

本会话从 Day 01 一直写到 Day 04,**dream 项目在公司电脑能做的事 100% 完成**——代码框架(2103 行)+ 完整文档(总方案、决策、4 份施工、4 份 references、简历、面试、demo、quickstart、代码自检)+ Mermaid 架构图 + 全部 polish。

剩下的工作**必须等用户回家**:装环境 / 修 9 个 1-line bug / 调 5 个 reflection prompt / dogfood / 录 demo / push GitHub。
